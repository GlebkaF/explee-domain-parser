# 🔧 GitHub Actions Cron - Troubleshooting

## Почему scheduled workflows могут не запускаться автоматически

### 1. **Первый запуск должен быть ручным**
GitHub иногда требует хотя бы один ручной запуск workflow перед тем, как начнут работать scheduled runs.

**Решение:**
1. Открой репозиторий на GitHub
2. Вкладка **Actions**
3. Выбери "Process Domains Cron"
4. Справа кнопка **"Run workflow"** → "Run workflow"

### 2. **GitHub требует активности репозитория**
Scheduled workflows работают только в активных репозиториях.

**Требования:**
- Репозиторий должен быть публичным ИЛИ
- Репозиторий приватный, но имел коммиты за последние 60 дней

### 3. **Default branch**
Scheduled workflows запускаются только из **default branch** (обычно `main` или `master`).

**Решение:**
Если ты на ветке `add-pagination-and-mock-agent`, нужно:
```bash
# Смержить в main
git checkout main
git merge add-pagination-and-mock-agent
git push origin main
```

### 4. **GitHub отключает неактивные workflows**
Если репозиторий был неактивен 60+ дней, GitHub отключает scheduled workflows.

**Проверить:**
Settings → Actions → General → Проверь что Actions включены

---

## ✅ Рекомендуемое решение для Hobby плана

Так как GitHub Actions scheduled workflows могут быть ненадежны, рекомендую **гибридный подход**:

### Включи автозапуск при клике "Run Agent"

Это гарантирует мгновенную обработку!

Раскомментируй строку в `app/api/domains/[id]/run-agent/route.ts`:

```typescript
// GitHub Actions будет обрабатывать домены каждую минуту
// Можно опционально добавить мгновенный триггер:
fetch(`${request.nextUrl.origin}/api/cron/process-domains`, { method: 'GET' }).catch(() => {});
```

Сделай так:

```typescript
// Мгновенный триггер обработки + GitHub Actions как бэкап
fetch(`${request.nextUrl.origin}/api/cron/process-domains`, { method: 'GET' }).catch(() => {});
```

---

## Альтернативы GitHub Actions

### 1. cron-job.org (100% надежно)
- Регистрация: https://cron-job.org
- Создай задачу на `https://explee-domain-parser.vercel.app/api/cron/process-domains`
- Расписание: каждую минуту
- Бесплатно, работает всегда

### 2. Vercel Cron (уже настроен)
- Раз в день (бесплатно на Hobby)
- Работает как резервный вариант

### 3. Ручной запуск
- GitHub Actions → Run workflow
- Или прямой вызов: `curl https://explee-domain-parser.vercel.app/api/cron/process-domains`

---

## Проверка работоспособности

### Ручной тест:
```bash
curl https://explee-domain-parser.vercel.app/api/cron/process-domains
```

Ответ должен быть примерно таким:
```json
{
  "success": true,
  "message": "Домен example.com успешно обработан",
  "processed": 1,
  "domain": "example.com"
}
```

Или если нет доменов в очереди:
```json
{
  "success": true,
  "message": "Нет доменов в очереди",
  "processed": 0
}
```

---

## Рекомендуемая настройка

✅ **Автозапуск в коде** - мгновенная реакция  
✅ **GitHub Actions** - бэкап (если запустятся)  
✅ **Vercel Cron** - резерв раз в день  
✅ **Поллинг UI** - показывает изменения в реальном времени  

Такая комбинация гарантирует работу в любом случае!

